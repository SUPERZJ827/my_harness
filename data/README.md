# Data Layout

This public snapshot keeps only `data/human_0/` as a lightweight sample task.

The complete benchmark dataset and larger evaluation artifacts should be downloaded separately instead of being committed to GitHub.

Reference:

- `https://huggingface.co/datasets/zd21/DataSciBench/tree/main`
